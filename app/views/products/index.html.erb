<div class="container">
  <ul id="product-sort">
    <li><%= link_to "All", products_path(category: @products[0].category) %></li>
    <% @brands.each do |b| %>
    <li><%= link_to b, products_path(brand: b, category: @products[0].category) %></li>
    <% end %>
  </ul>
  <!-- <%= link_to "All products", products_path %> -->
  <div class="row">
    <% @products.each do |product| %>
      <div class="product-pane col-sm-3">
          <h4><%= link_to "#{product.brand} #{product.name}", product %></h4>
          <%= link_to image_tag(product.image.url(:thumb), class: 'center-block'), product %>

        <% if current_user.admin == true %>
          <%= link_to 'Edit', edit_product_path(product) %> |
          <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>
        <% end %>
        </tr>
      </div>
    <% end %>
  </div>
  <% if current_user.admin == true %>
    <div class="row">
      <%= link_to 'New Product', new_product_path %>
    </div>
  <% end %>
</div>
